{
  "name": "@financeflow/shared",
  "version": "1.0.0",
  "description": "Código compartido entre apps de FinanceFlow AI (types, utils, constants)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src --ext .ts",
    "test": "vitest"
  },
  "dependencies": {
    "zod": "^3.22.4",
    "date-fns": "^3.0.6"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "eslint": "^8.55.0",
    "vitest": "^1.1.0"
  }
}
